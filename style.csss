:root {
    --bg-main: #0a0a0a;
    --bg-card: #141414;
    --border-color: #262626;
    --text-main: #ffffff;
    --text-muted: #8a8a8a;
}
* { margin: 0; padding: 0; box-sizing: border-box; }
body { background-color: var(--bg-main); color: var(--text-main); font-family: sans-serif; line-height: 1.6; }
a { color: inherit; text-decoration: none; }

.navbar { display: flex; justify-content: space-between; padding: 20px 5%; border-bottom: 1px solid var(--border-color); }
.logo { font-weight: 800; letter-spacing: 2px; }
.nav-links a { margin-left: 20px; color: var(--text-muted); }

.tattoo-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 30px; padding: 40px 5%; }
.tattoo-card { background-color: var(--bg-card); border: 1px solid var(--border-color); border-radius: 4px; display: block; }
.tattoo-image-wrapper { background: #fff; height: 300px; display: flex; align-items: center; justify-content: center; }
.tattoo-image-wrapper img { max-width: 90%; max-height: 90%; object-fit: contain; }
.tattoo-info { padding: 15px; }

.btn-primary { background: #fff; color: #000; padding: 10px 20px; border: none; cursor: pointer; font-weight: 600; }
